{
  "name": "array-divider",
  "version": "1.0.0",
  "description": "Array Divider divides an array into equally sized sub arrays for a given bucket size.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "mocha src/**/*.spec.js --recursive --watch"
  },
  "author": "Ben Jovanic",
  "license": "MIT",
  "devDependencies": {
    "chai": "4.1.2"
  }
}
